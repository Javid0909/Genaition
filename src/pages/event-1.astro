<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Build your first AI Email Reply Agent — Online · 13 June</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <script defer src="https://cloud.umami.is/script.js" data-website-id="045fc1e4-9f56-44f2-992e-98f5fb7717a5"></script>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:wght@300;400;500;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">

  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #faf9f5;
      --bg-alt: #f2f0ea;
      --ink: #1a1208;
      --ink-2: rgba(26,18,8,0.62);
      --ink-3: rgba(26,18,8,0.38);
      --green: #1a6b3a;
      --green-bg: rgba(26,107,58,0.08);
      --green-border: rgba(26,107,58,0.22);
      --green-glow: rgba(26,107,58,0.18);
      --border: rgba(26,18,8,0.10);
      --border-s: rgba(26,18,8,0.18);
      --dark: #0d0d0d;
      --white: #ffffff;
      --strip: 32px;
      --nav: 60px;
      --hdr: 92px;
      --r: 3px;
    }

    html { scroll-behavior: smooth; }

    /* Grain texture overlay */
    body {
      background: var(--bg);
      color: var(--ink);
      font-family: 'DM Sans', sans-serif;
      font-size: 16px;
      line-height: 1.6;
      overflow-x: hidden;
    }
    body::before {
      content: '';
      position: fixed; inset: 0; z-index: 9999;
      pointer-events: none;
      opacity: 0.028;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      background-size: 200px 200px;
    }

    section, [id] { scroll-margin-top: calc(var(--hdr) + 12px); }

    /* ── SCROLL REVEAL ── */
    .reveal {
      opacity: 0;
      transform: translateY(28px);
      transition: opacity 0.7s cubic-bezier(.22,.68,0,1.2), transform 0.7s cubic-bezier(.22,.68,0,1.2);
    }
    .reveal.in { opacity: 1; transform: translateY(0); }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }
    .reveal-delay-4 { transition-delay: 0.4s; }

    /* ── PUBLICATION STRIP — MARQUEE ── */
    .pub-strip {
      position: fixed; top: 0; left: 0; right: 0; height: var(--strip);
      background: var(--ink); z-index: 200;
      display: flex; align-items: center;
      overflow: hidden;
    }
    .pub-marquee {
      display: flex; align-items: center;
      white-space: nowrap;
      animation: marquee 28s linear infinite;
      gap: 0;
    }
    .pub-marquee-item {
      font-family: 'DM Mono', monospace;
      font-size: 0.52rem; letter-spacing: 0.2em; text-transform: uppercase;
      color: rgba(255,255,255,0.42); padding: 0 2.5rem;
    }
    .pub-marquee-item .dot {
      display: inline-block; width: 4px; height: 4px; background: var(--green);
      border-radius: 50%; margin: 0 1.2rem; vertical-align: middle;
    }
    @keyframes marquee {
      0%   { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    /* ── SITE NAV ── */
    .site-nav {
      position: fixed; top: var(--strip); left: 0; right: 0; height: var(--nav);
      background: rgba(250,249,245,0.96);
      backdrop-filter: blur(18px);
      border-bottom: 1px solid var(--border-s);
      z-index: 199; display: flex; align-items: center;
      transition: box-shadow 0.3s;
    }
    .site-nav.scrolled { box-shadow: 0 2px 24px rgba(26,18,8,0.08); }
    .nav-inner {
      max-width: 1200px; margin: 0 auto; padding: 0 2.5rem;
      width: 100%; display: flex; align-items: center; justify-content: space-between;
    }
    .logo { display: flex; align-items: center; gap: 0.75rem; text-decoration: none; }
    .logo-mark {
      width: 34px; height: 34px; border-radius: 2px;
      overflow: hidden;
      display: flex; align-items: center; justify-content: center;
    }
    .logo-mark img { width: 100%; height: 100%; object-fit: cover; }
    .logo-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.25rem; letter-spacing: 0.1em; color: var(--ink); line-height: 1; }
    .logo-sub { font-family: 'DM Mono', monospace; font-size: 0.46rem; text-transform: uppercase; letter-spacing: 0.22em; color: var(--ink-3); display: block; margin-top: 2px; }
    .nav-links { display: flex; align-items: center; gap: 2rem; }
    .nav-links a {
      font-family: 'DM Mono', monospace; font-size: 0.6rem; text-transform: uppercase;
      letter-spacing: 0.16em; color: var(--ink-2); text-decoration: none;
      transition: color 0.2s; position: relative;
    }
    .nav-links a::after {
      content: ''; position: absolute; bottom: -3px; left: 0; right: 0;
      height: 1px; background: var(--green);
      transform: scaleX(0); transform-origin: left;
      transition: transform 0.25s ease;
    }
    .nav-links a:hover { color: var(--green); }
    .nav-links a:hover::after { transform: scaleX(1); }
    .nav-actions { display: flex; align-items: center; gap: 0.85rem; }
    .nav-signup {
      background: #f0c000; color: var(--ink);
      font-family: 'DM Sans', sans-serif; font-weight: 600; font-size: 0.72rem;
      text-transform: uppercase; letter-spacing: 0.12em;
      padding: 0.52rem 1.25rem; border-radius: 2px; text-decoration: none;
      white-space: nowrap;
      transition: background 0.2s, transform 0.15s;
    }
    .nav-signup:hover { background: #d4aa00; transform: translateY(-1px); }

    .lang-wrap { position: relative; }
    .lang-btn { display: flex; align-items: center; gap: 0.4rem; cursor: pointer; font-family: 'DM Mono', monospace; font-size: 0.58rem; text-transform: uppercase; letter-spacing: 0.14em; color: var(--ink-2); background: none; border: 1px solid var(--border-s); border-radius: 2px; padding: 0.32rem 0.65rem; transition: color 0.2s, border-color 0.2s; }
    .lang-btn:hover { color: var(--ink); border-color: var(--border); }
    .lang-btn img { width: 16px; height: 16px; border-radius: 2px; object-fit: cover; }
    .lang-btn svg { width: 11px; height: 11px; stroke: currentColor; fill: none; }
    .lang-menu { display: none; position: absolute; right: 0; top: calc(100% + 6px); min-width: 140px; background: var(--bg); border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden; z-index: 300; box-shadow: 0 8px 32px rgba(26,18,8,0.12); }
    .lang-wrap.open .lang-menu { display: block; }
    .lang-menu a { display: flex; align-items: center; gap: 0.55rem; padding: 0.6rem 0.9rem; font-family: 'DM Mono', monospace; font-size: 0.58rem; text-transform: uppercase; letter-spacing: 0.14em; color: var(--ink-2); text-decoration: none; transition: background 0.15s, color 0.15s; }
    .lang-menu a:hover { background: var(--bg-alt); color: var(--green); }
    .lang-menu a img { width: 15px; height: 15px; border-radius: 2px; }

    @media (max-width: 700px) { .nav-links { display: none; } .nav-inner { padding: 0 1.25rem; } }

    /* ── HERO ── */
    .hero {
      padding-top: var(--hdr);
      background: var(--dark) url('/aiagentworkshop1.png') center center / cover no-repeat;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      position: relative;
    }
    @media (max-width: 640px) {
      .hero { background-image: url('/aiagentworkshop.png'); }
    }
    .hero::before {
      content: '';
      position: absolute; inset: 0; z-index: 0;
      background: linear-gradient(100deg, rgba(8,6,4,0.92) 0%, rgba(8,6,4,0.82) 52%, rgba(8,6,4,0.68) 100%);
      pointer-events: none;
    }
    .hero::after {
      content: '';
      position: absolute; top: var(--hdr); right: 0; width: 600px; height: 500px;
      background: radial-gradient(ellipse at 80% 20%, rgba(26,107,58,0.16) 0%, transparent 65%);
      pointer-events: none; z-index: 1;
    }
    .hero-grid {
      max-width: 860px; margin: 0 auto; padding: 5rem 2.5rem 5.5rem;
      display: grid; grid-template-columns: 1fr;
      gap: 3rem; align-items: start; position: relative; z-index: 1;
    }
    @media (max-width: 640px) { .hero-grid { padding: 3rem 1.25rem 2.5rem; } }

    .hero-left { display: flex; flex-direction: column; align-items: flex-start; }

    .hero-eyebrow {
      display: inline-flex; align-items: center; gap: 0.55rem;
      font-family: 'DM Mono', monospace; font-size: 0.62rem; letter-spacing: 0.2em; text-transform: uppercase;
      color: #f0c000; border: 1px solid rgba(240,192,0,0.35); background: rgba(240,192,0,0.08);
      padding: 0.3rem 0.8rem; border-radius: 2px; margin-bottom: 1.5rem;
      animation: fadeUp 0.6s ease 0.1s both;
    }
    .eyebrow-dot { width: 6px; height: 6px; background: #f0c000; border-radius: 50%; animation: pulse 2s ease infinite; display: inline-block; flex-shrink: 0; }
    @keyframes pulse { 0%,100%{opacity:1; transform:scale(1);} 50%{opacity:0.3; transform:scale(0.8);} }

    .hero-title {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(4rem, 7.5vw, 7rem);
      font-weight: 400; line-height: 0.92; letter-spacing: 0.02em;
      color: var(--ink); margin-bottom: 1.8rem;
      animation: fadeUp 0.7s ease 0.2s both;
    }
    .hero-title .t-ink { display: block; color: var(--white); }
    .hero-title .t-yellow { display: block; color: #f0c000; font-style: normal; }

    .hero-quote {
      border-left: 3px solid var(--green); padding-left: 1.25rem; margin-bottom: 2rem;
      font-family: 'DM Sans', sans-serif; font-style: normal; font-weight: 300;
      font-size: 1.08rem; color: rgba(255,255,255,0.72); line-height: 1.75; max-width: 540px;
      animation: fadeUp 0.6s ease 0.3s both;
    }

    .hero-mobile-cta {
      display: none;
      margin-bottom: 1.5rem;
      width: 100%;
    }
    @media (max-width: 640px) {
      .hero-mobile-cta { display: flex; }
    }

    /* ── COUNTDOWN ── */
    .hero-countdown { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 1.5rem; animation: fadeUp 0.6s ease 0.35s both; }
    .countdown-label { font-family: 'DM Mono', monospace; font-size: 0.55rem; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(255,255,255,0.45); white-space: nowrap; }
    .countdown-unit { display: flex; flex-direction: column; align-items: center; gap: 0.08rem; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); padding: 0.35rem 0.5rem; border-radius: 2px; min-width: 40px; }
    .countdown-num { font-family: 'Bebas Neue', sans-serif; font-size: 1.35rem; font-weight: 400; letter-spacing: 0.04em; color: var(--white); line-height: 1; }
    .countdown-sub { font-family: 'DM Mono', monospace; font-size: 0.42rem; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(255,255,255,0.35); }
    .countdown-sep { font-family: 'Bebas Neue', sans-serif; font-size: 1.2rem; color: rgba(255,255,255,0.3); }

    .hero-rule { width: 100%; height: 1px; background: rgba(255,255,255,0.12); margin-bottom: 1.75rem; animation: fadeUp 0.5s ease 0.4s both; }

    .hero-meta {
      display: grid; grid-template-columns: repeat(4, 1fr);
      gap: 0; border: 1px solid rgba(255,255,255,0.12); border-radius: 2px;
      overflow: hidden; width: 100%; margin-bottom: 0;
      animation: fadeUp 0.5s ease 0.45s both;
    }
    @media (max-width: 500px) { .hero-meta { grid-template-columns: 1fr 1fr; } }
    .meta-block {
      padding: 0.85rem 1rem; border-right: 1px solid rgba(255,255,255,0.1);
      background: rgba(13,13,13,0.5); backdrop-filter: blur(8px); transition: background 0.2s;
    }
    .meta-block:hover { background: rgba(13,13,13,0.68); }
    .meta-block:last-child { border-right: none; }
    .meta-label { font-family: 'DM Mono', monospace; font-size: 0.48rem; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(255,255,255,0.38); margin-bottom: 0.2rem; }
    .meta-value { font-family: 'Bebas Neue', sans-serif; font-size: 1.15rem; font-weight: 400; letter-spacing: 0.04em; color: var(--white); line-height: 1.2; }
    .meta-value.green { color: var(--green); }
    .meta-value.yellow { color: #f0c000; }

    /* ── HERO EVENT CARD ── */
    .hero-right { animation: fadeUp 0.7s ease 0.3s both; }
    .hero-event-card {
      border: 1px solid rgba(255,255,255,0.12); border-radius: 2px; overflow: hidden;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }
    .hero-event-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }

    /* product screenshot in card — no photo filter */
    .hero-event-photo.is-product img {
      filter: none;
      object-position: top center;
    }
    .hero-event-photo.is-product .hero-event-photo-overlay {
      background: linear-gradient(to bottom, transparent 55%, rgba(13,13,13,0.7) 100%);
    }
    .hero-event-photo { height: 300px; overflow: hidden; position: relative; }
    .hero-event-photo img {
      width: 100%; height: 100%; object-fit: cover; object-position: center 30%;
      filter: sepia(0.15) brightness(0.78) contrast(1.08) saturate(0.85);
      display: block; transition: transform 0.6s ease;
    }
    .hero-event-card:hover .hero-event-photo img { transform: scale(1.04); }
    .hero-event-photo-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(to bottom, rgba(13,13,13,0.05) 0%, rgba(13,13,13,0.72) 100%);
    }
    .hero-event-photo-caption {
      position: absolute; bottom: 0.85rem; left: 1rem;
      font-family: 'DM Mono', monospace; font-size: 0.48rem; letter-spacing: 0.2em;
      text-transform: uppercase; color: rgba(255,255,255,0.45);
    }
    /* Green bar on top of card */
    .hero-event-card::before {
      content: ''; display: block; height: 3px;
      background: linear-gradient(90deg, var(--green), #2d8a52);
    }

    /* ── HERO TICKET ── */
    .hero-ticket {
      background: var(--dark); border-top: none;
      padding: 1.75rem 1.5rem 1.5rem;
    }
    .ticket-price-old { font-family: 'DM Mono', monospace; font-size: 0.9rem; color: rgba(255,255,255,0.28); text-decoration: line-through; margin-bottom: 0.2rem; }
    .ticket-price-row { display: flex; align-items: baseline; gap: 0.85rem; flex-wrap: wrap; margin-bottom: 0.35rem; }
    .ticket-price { font-family: 'Bebas Neue', sans-serif; font-size: 3rem; font-weight: 400; letter-spacing: 0.02em; color: var(--white); line-height: 1; }
    .ticket-save { font-family: 'DM Mono', monospace; font-size: 0.56rem; text-transform: uppercase; letter-spacing: 0.15em; color: #d4aa00; background: rgba(212,170,0,0.14); border: 1px solid rgba(212,170,0,0.38); padding: 0.18rem 0.5rem; border-radius: 2px; white-space: nowrap; }
    .ticket-divider { height: 1px; background: rgba(255,255,255,0.08); margin: 1.2rem 0; }
    .ticket-features { list-style: none; margin-bottom: 1.25rem; display: flex; flex-direction: column; gap: 0; }
    .ticket-features li { display: flex; gap: 0.75rem; font-size: 0.84rem; color: rgba(255,255,255,0.58); padding: 0.42rem 0; border-bottom: 1px solid rgba(255,255,255,0.06); transition: color 0.2s; }
    .ticket-features li:hover { color: rgba(255,255,255,0.85); }
    .ticket-features li:last-child { border-bottom: none; }
    .ticket-features li::before { content: '—'; color: var(--green); flex-shrink: 0; font-size: 0.75rem; margin-top: 2px; }

    /* Seat progress bar */
    .seat-progress-wrap { margin-bottom: 1.1rem; }
    .seat-progress-label {
      display: flex; justify-content: space-between; align-items: center;
      font-family: 'DM Mono', monospace; font-size: 0.55rem; letter-spacing: 0.14em;
      text-transform: uppercase; color: rgba(255,255,255,0.35); margin-bottom: 0.45rem;
    }
    .seat-progress-label .urgent { color: #e8624a; animation: urgency 2s ease infinite; }
    @keyframes urgency { 0%,100%{opacity:1} 50%{opacity:0.5} }
    .seat-bar { height: 3px; background: rgba(255,255,255,0.08); border-radius: 2px; overflow: hidden; }
    .seat-bar-fill {
      height: 100%; width: 72%; background: linear-gradient(90deg, var(--green), #2d8a52);
      border-radius: 2px;
      animation: fillIn 1.2s ease 0.8s both;
    }
    @keyframes fillIn { from { width: 0; } to { width: 72%; } }

    .ticket-cta {
      display: flex; align-items: center; justify-content: center; gap: 0.6rem;
      width: 100%; padding: 0.95rem 2rem; background: #f0c000; color: var(--ink);
      font-family: 'DM Sans', sans-serif; font-weight: 700; font-size: 0.88rem;
      text-transform: uppercase; letter-spacing: 0.14em; text-decoration: none;
      border-radius: 2px; transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
      position: relative; overflow: hidden;
    }
    .ticket-cta::after {
      content: ''; position: absolute; inset: 0;
      background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.2) 50%, transparent 70%);
      transform: translateX(-100%);
      transition: transform 0.6s ease;
    }
    .ticket-cta:hover { background: #d4aa00; transform: translateY(-1px); box-shadow: 0 8px 28px rgba(240,192,0,0.35); }
    .ticket-cta:hover::after { transform: translateX(100%); }
    .ticket-note { margin-top: 0.8rem; font-family: 'DM Mono', monospace; font-size: 0.54rem; text-transform: uppercase; letter-spacing: 0.14em; color: rgba(255,255,255,0.25); text-align: center; }
    .ticket-note a { color: rgba(255,255,255,0.32); text-decoration: underline; }


    /* ── STAT BAR ── */
    .stat-bar { background: var(--ink); }
    .stat-bar-inner {
      max-width: 1200px; margin: 0 auto; padding: 0 2.5rem;
      display: grid; grid-template-columns: repeat(4, 1fr);
    }
    .stat-item {
      padding: 1.75rem 1rem; border-right: 1px solid rgba(255,255,255,0.08);
      display: flex; flex-direction: column; align-items: center; gap: 0.25rem; text-align: center;
      transition: background 0.2s;
    }
    .stat-item:hover { background: rgba(255,255,255,0.03); }
    .stat-item:last-child { border-right: none; }
    .stat-num { font-family: 'Bebas Neue', sans-serif; font-size: 2.4rem; font-weight: 400; letter-spacing: 0.02em; color: var(--white); line-height: 1; }
    .stat-num .stat-accent { color: var(--green); }
    .stat-label { font-family: 'DM Mono', monospace; font-size: 0.46rem; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(255,255,255,0.3); }
    @media (max-width: 640px) {
      .stat-bar-inner { grid-template-columns: 1fr 1fr; padding: 0 1.25rem; }
      .stat-item:nth-child(2) { border-right: none; }
      .stat-item:nth-child(1), .stat-item:nth-child(2) { border-bottom: 1px solid rgba(255,255,255,0.08); }
    }


    /* ── SECTIONS ── */
    .section { padding: 5.5rem 0; }
    .section-alt { background: var(--bg-alt); }
    .container { max-width: 760px; margin: 0 auto; padding: 0 2rem; }
    @media (max-width: 640px) { .container { padding: 0 1.25rem; } }

    .sec-eyebrow {
      font-family: 'DM Mono', monospace; font-size: 0.6rem; letter-spacing: 0.22em;
      text-transform: uppercase; color: var(--green); margin-bottom: 0.6rem;
      display: flex; align-items: center; gap: 0.6rem;
    }
    .sec-eyebrow::before { content: ''; display: inline-block; width: 18px; height: 1px; background: var(--green); }
    .sec-title {
      font-family: 'Bebas Neue', sans-serif; font-size: clamp(2.6rem, 4.5vw, 3.8rem);
      font-weight: 400; line-height: 1.0; letter-spacing: 0.02em; color: var(--ink); margin-bottom: 1rem;
    }
    .sec-title em { font-style: normal; color: var(--green); }
    .sec-desc { font-size: 0.97rem; color: var(--ink-2); line-height: 1.82; font-weight: 300; }
    .sec-rule { height: 1px; background: var(--border); margin: 2rem 0; }

    /* ── DARK BENEFIT CARDS ── */
    .dark-strip {
      background: var(--dark);
      margin: 2.5rem 0;
      padding-bottom: 2.5rem;
    }
    .dark-strip-footer {
      max-width: 760px; margin: 0 auto; padding: 0 2rem;
    }
    @media (max-width: 640px) { .dark-strip-footer { padding: 0 1.25rem; } }
    .benefit-dark-grid {
      display: grid; grid-template-columns: repeat(4, 1fr);
    }
    .benefit-dark-card {
      padding: 3rem 2.5rem; border-right: 1px solid rgba(255,255,255,0.07);
      transition: background 0.25s;
      position: relative; overflow: hidden;
    }
    .benefit-dark-card::after {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
      background: var(--green); transform: scaleX(0); transform-origin: left;
      transition: transform 0.3s ease;
    }
    .benefit-dark-card:hover { background: rgba(255,255,255,0.025); }
    .benefit-dark-card:hover::after { transform: scaleX(1); }
    .benefit-dark-card:last-child { border-right: none; }
    .bdc-icon {
      width: 40px; height: 40px; border: 1px solid rgba(26,107,58,0.3); border-radius: 3px;
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 1.1rem; background: rgba(26,107,58,0.08);
      transition: background 0.2s, border-color 0.2s;
    }
    .benefit-dark-card:hover .bdc-icon { background: rgba(26,107,58,0.18); border-color: rgba(26,107,58,0.5); }
    .bdc-icon svg { width: 18px; height: 18px; stroke: var(--green); fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
    .bdc-title { font-family: 'DM Sans', sans-serif; font-weight: 700; font-size: 0.82rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--white); margin-bottom: 0.6rem; }
    .bdc-desc { font-size: 0.83rem; color: rgba(255,255,255,0.45); line-height: 1.65; }
    @media (max-width: 860px) {
      .benefit-dark-grid { grid-template-columns: 1fr 1fr; }
      .benefit-dark-card:nth-child(2) { border-right: none; }
      .benefit-dark-card:nth-child(1), .benefit-dark-card:nth-child(2) { border-bottom: 1px solid rgba(255,255,255,0.07); }
    }
    @media (max-width: 480px) {
      .benefit-dark-grid { grid-template-columns: 1fr; }
      .benefit-dark-card { border-right: none; border-bottom: 1px solid rgba(255,255,255,0.07); }
      .benefit-dark-card:last-child { border-bottom: none; }
    }

    /* ── TECH REVEAL ── */
    .tech-reveal { margin-top: 2rem; border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden; }
    .tech-reveal-btn { width: 100%; background: var(--bg); border: none; color: var(--ink); padding: 1rem 1.25rem; display: flex; justify-content: space-between; align-items: center; cursor: pointer; font-family: 'DM Mono', monospace; font-size: 0.62rem; text-transform: uppercase; letter-spacing: 0.16em; transition: background 0.2s; }
    .tech-reveal-btn:hover { background: var(--bg-alt); color: var(--green); }
    .tech-reveal-btn svg { width: 15px; height: 15px; stroke: var(--green); fill: none; transition: transform 0.3s; flex-shrink: 0; }
    .tech-reveal-btn.open svg { transform: rotate(45deg); }
    .tech-reveal-body { max-height: 0; overflow: hidden; transition: max-height 0.35s ease; background: var(--bg-alt); }
    .tech-reveal-inner { padding: 1.4rem 1.5rem; font-size: 0.88rem; color: var(--ink-2); line-height: 1.8; border-top: 1px solid var(--border); }
    .tech-reveal-inner strong { color: var(--green); }
    .dark-strip .tech-reveal { border-color: rgba(255,255,255,0.10); margin-top: 2.5rem; }
    .dark-strip .tech-reveal-btn { background: rgba(255,255,255,0.04); color: rgba(255,255,255,0.65); }
    .dark-strip .tech-reveal-btn:hover { background: rgba(255,255,255,0.07); }
    .dark-strip .tech-reveal-body { background: rgba(255,255,255,0.02); }
    .dark-strip .tech-reveal-inner { color: rgba(255,255,255,0.55); border-top-color: rgba(255,255,255,0.08); }

    /* ── BUILD ROWS ── */
    #build .container { max-width: 960px; }
    .build-rows { margin-top: 3rem; display: flex; flex-direction: column; gap: 0; border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden; }
    .build-row { display: grid; grid-template-columns: 1fr 1fr; }
    .build-row.reverse .build-photo-side { order: 2; }
    .build-row.reverse .build-text-side { order: 1; }
    .build-text-side { padding: 2.25rem 2rem; border-left: 1px solid var(--border); }
    .build-row.reverse .build-text-side { border-left: none; border-right: 1px solid var(--border); }
    .build-photo-side { position: relative; overflow: hidden; min-height: 300px; height: auto; }
    .build-photo-side img {
      width: 100%; height: 100%; object-fit: cover; object-position: center;
      filter: sepia(0.2) brightness(0.82) contrast(1.05) saturate(0.82);
      transition: transform 0.6s ease, filter 0.4s ease;
      display: block; position: absolute; inset: 0;
    }
    .build-row:hover .build-photo-side img {
      transform: scale(1.05);
      filter: sepia(0.1) brightness(0.9) contrast(1.03) saturate(0.9);
    }
    .build-photo-label { position: absolute; bottom: 0; left: 0; right: 0; padding: 0.75rem 1rem; background: linear-gradient(transparent, rgba(26,18,8,0.55)); font-family: 'DM Mono', monospace; font-size: 0.5rem; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(255,255,255,0.5); z-index: 1; }
    .build-num { font-family: 'Bebas Neue', sans-serif; font-size: 2.8rem; font-weight: 400; letter-spacing: 0.04em; color: var(--green); opacity: 0.22; line-height: 1; margin-bottom: 0.35rem; transition: opacity 0.2s; }
    .build-row:hover .build-num { opacity: 0.4; }
    .build-title { font-weight: 700; font-size: 0.88rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--ink); margin-bottom: 0.4rem; }
    .build-desc { font-size: 0.86rem; color: var(--ink-2); line-height: 1.65; margin-bottom: 1rem; }
    .build-checks { list-style: none; }
    .build-checks li { display: flex; gap: 0.6rem; font-size: 0.84rem; color: var(--ink-2); padding: 0.42rem 0; border-bottom: 1px solid var(--border); transition: color 0.2s, padding-left 0.2s; }
    .build-checks li:hover { color: var(--ink); padding-left: 4px; }
    .build-checks li:last-child { border-bottom: none; }
    .build-checks li::before { content: '—'; color: var(--green); flex-shrink: 0; font-size: 0.75rem; margin-top: 2px; }
    .build-divider { height: 1px; background: var(--border); }
    @media (max-width: 720px) {
      .build-row, .build-row.reverse { grid-template-columns: 1fr; }
      .build-photo-side { order: -1 !important; min-height: 220px; height: 220px; position: relative; }
      .build-text-side, .build-row.reverse .build-text-side { border-left: none; border-right: none; border-top: 1px solid var(--border); }
    }

    /* ── AGENDA ── */
    .agenda-list { margin-top: 2.5rem; display: flex; flex-direction: column; gap: 0; border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden; }
    .agenda-item { display: grid; grid-template-columns: 90px 1fr; gap: 0; border-bottom: 1px solid var(--border); transition: background 0.2s; }
    .agenda-item:hover { background: rgba(26,107,58,0.04); }
    .agenda-item:last-child { border-bottom: none; }
    .agenda-time-col { background: var(--bg-alt); border-right: 1px solid var(--border); padding: 1.4rem 1rem; display: flex; align-items: flex-start; justify-content: center; transition: background 0.2s; }
    .agenda-item:hover .agenda-time-col { background: rgba(26,107,58,0.08); }
    .agenda-time { font-family: 'Bebas Neue', sans-serif; font-style: normal; font-size: 1.3rem; letter-spacing: 0.06em; color: var(--green); line-height: 1; }
    .agenda-content { padding: 1.3rem 1.5rem; }
    .agenda-topic { font-weight: 700; font-size: 0.88rem; text-transform: uppercase; letter-spacing: 0.07em; color: var(--ink); margin-bottom: 0.3rem; }
    .agenda-detail { font-size: 0.84rem; color: var(--ink-2); line-height: 1.6; }

    /* ── INSTRUCTOR ── */
    .instructor-grid { display: grid; grid-template-columns: 220px 1fr; gap: 3rem; align-items: start; margin-top: 2.5rem; }
    @media (max-width: 640px) { .instructor-grid { grid-template-columns: 1fr; gap: 1.75rem; } }
    .instructor-photo-wrap {
      position: relative; border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden;
      transition: box-shadow 0.3s, transform 0.3s;
    }
    .instructor-photo-wrap:hover { box-shadow: 0 12px 40px rgba(26,18,8,0.12); transform: translateY(-2px); }
    .instructor-photo-wrap img { display: block; width: 100%; aspect-ratio: 3/4; object-fit: cover; object-position: center top; filter: sepia(0.15) brightness(0.9) contrast(1.04) saturate(0.85); transition: transform 0.5s ease; }
    .instructor-photo-wrap:hover img { transform: scale(1.03); }
    .instructor-plate { padding: 1rem 1rem 0.85rem; border-top: 1px solid var(--border-s); background: var(--bg-alt); }
    .instructor-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.4rem; font-weight: 400; letter-spacing: 0.08em; color: var(--ink); line-height: 1.1; }
    .instructor-role { font-family: 'DM Mono', monospace; font-size: 0.54rem; text-transform: uppercase; letter-spacing: 0.18em; color: var(--green); margin-top: 0.2rem; }
    .instructor-badges { display: flex; gap: 0.4rem; flex-wrap: wrap; margin-top: 0.6rem; }
    .ibadge { padding: 0.18rem 0.55rem; border-radius: 2px; font-family: 'DM Mono', monospace; font-size: 0.52rem; letter-spacing: 0.12em; text-transform: uppercase; background: var(--green-bg); color: var(--green); border: 1px solid var(--green-border); }
    .instructor-bio { font-size: 0.95rem; color: var(--ink-2); line-height: 1.82; font-weight: 300; }
    .instructor-bio + .instructor-bio { margin-top: 0.85rem; }
    .instructor-pullquote {
      margin-top: 1.5rem; border-left: 3px solid var(--green); padding-left: 1.1rem;
      font-family: 'DM Sans', sans-serif; font-style: normal; font-weight: 300;
      font-size: 1rem; color: var(--ink); line-height: 1.7;
    }

    /* ── GALLERY ── */
    .gallery-section { padding: 5rem 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); background: var(--bg-alt); }
    .gallery-header { display: flex; align-items: flex-end; justify-content: space-between; margin-bottom: 2rem; flex-wrap: wrap; gap: 1rem; }
    .gallery-count { font-family: 'DM Mono', monospace; font-size: 0.56rem; letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-3); }
    .gallery-grid { display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: auto auto; gap: 0.65rem; max-width: 760px; margin: 0 auto; padding: 0 2rem; }
    @media (max-width: 640px) { .gallery-grid { grid-template-columns: 1fr; padding: 0 1.25rem; } .gallery-slot-a { grid-row: auto; min-height: unset; } }
    .gallery-slot-a { grid-row: 1 / 3; position: relative; overflow: hidden; min-height: 360px; border: 1px solid var(--border-s); }
    .gallery-slot-a img { width: 100%; height: 100%; object-fit: cover; object-position: center top; display: block; filter: sepia(0.18) brightness(0.88) contrast(1.04) saturate(0.82); transition: transform 0.6s ease, filter 0.4s ease; }
    .gallery-slot-a:hover img { transform: scale(1.04); filter: sepia(0.08) brightness(0.95) contrast(1.02) saturate(0.9); }
    .gallery-slot-b, .gallery-slot-c { position: relative; overflow: hidden; aspect-ratio: 16/9; border: 1px solid var(--border-s); }
    .gallery-slot-b img, .gallery-slot-c img { width: 100%; height: 100%; object-fit: cover; display: block; filter: sepia(0.18) brightness(0.88) contrast(1.04) saturate(0.82); transition: transform 0.6s ease, filter 0.4s ease; }
    .gallery-slot-b:hover img, .gallery-slot-c:hover img { transform: scale(1.04); filter: sepia(0.08) brightness(0.95) contrast(1.02) saturate(0.9); }
    .gallery-img-label { position: absolute; bottom: 0; left: 0; right: 0; padding: 0.75rem 0.85rem; background: linear-gradient(transparent, rgba(26,18,8,0.55)); font-family: 'DM Mono', monospace; font-size: 0.48rem; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(255,255,255,0.48); pointer-events: none; }

    /* ── TESTIMONIALS ── */
    .tp-section { padding: 5.5rem 0; background: var(--bg); border-top: 1px solid var(--border); }
    .tp-cards {
      margin-top: 2.5rem; display: grid; grid-template-columns: 1fr 1fr 1fr;
      border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden;
    }
    .tp-card { padding: 1.75rem 1.75rem 1.5rem; border-right: 1px solid var(--border); position: relative; transition: background 0.2s; }
    .tp-card:hover { background: var(--bg-alt); }
    .tp-card:last-child { border-right: none; }
    .tp-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
      background: var(--green); transform: scaleX(0); transform-origin: left;
      transition: transform 0.3s ease;
    }
    .tp-card:hover::before { transform: scaleX(1); }
    @media (max-width: 700px) {
      .tp-cards { grid-template-columns: 1fr; }
      .tp-card { border-right: none; border-bottom: 1px solid var(--border); }
      .tp-card:last-child { border-bottom: none; }
    }
    .tp-card-stars { display: flex; gap: 3px; margin-bottom: 1rem; }
    .tp-star { width: 14px; height: 14px; background: var(--green); clip-path: polygon(50% 0%,61% 35%,98% 35%,68% 57%,79% 91%,50% 70%,21% 91%,32% 57%,2% 35%,39% 35%); flex-shrink: 0; }
    .tp-quotemark { font-family: 'Bebas Neue', sans-serif; font-size: 4.5rem; color: var(--green); opacity: 0.15; line-height: 0.6; margin-bottom: 0.6rem; display: block; }
    .tp-card-text { font-family: 'DM Sans', sans-serif; font-style: normal; font-weight: 300; font-size: 0.98rem; color: var(--ink); line-height: 1.78; margin-bottom: 1rem; }
    .tp-card-name { font-family: 'DM Sans', sans-serif; font-size: 0.84rem; font-weight: 700; color: var(--ink); }
    .tp-card-date { font-family: 'DM Mono', monospace; font-size: 0.52rem; letter-spacing: 0.14em; text-transform: uppercase; color: var(--ink-3); margin-top: 0.1rem; }

    /* ── FAQ ── */
    .faq-list { margin-top: 2.5rem; border: 1px solid var(--border-s); border-radius: 2px; overflow: hidden; }
    .faq-item { border-bottom: 1px solid var(--border); }
    .faq-item:last-child { border-bottom: none; }
    .faq-btn { width: 100%; background: none; border: none; color: var(--ink); padding: 1.1rem 1.25rem; display: flex; justify-content: space-between; align-items: center; text-align: left; cursor: pointer; font-family: 'DM Sans', sans-serif; font-size: 0.9rem; font-weight: 500; gap: 1rem; transition: color 0.2s, background 0.15s; }
    .faq-btn span { flex: 1; min-width: 0; }
    .faq-btn:hover { color: var(--green); background: var(--bg-alt); }
    .faq-icon { width: 15px; height: 15px; flex-shrink: 0; stroke: var(--green); fill: none; transition: transform 0.3s; }
    .faq-item.open .faq-icon { transform: rotate(45deg); }
    .faq-body { max-height: 0; overflow: hidden; transition: max-height 0.35s ease; }
    .faq-body-inner { padding: 0 1.25rem 1.2rem; font-size: 0.88rem; color: var(--ink-2); line-height: 1.82; }
    .faq-body-inner a { color: var(--green); text-decoration: none; border-bottom: 1px solid var(--green-border); }

    /* ── STICKY BAR ── */
    .sticky-bar { position: fixed; bottom: 0; left: 0; right: 0; z-index: 190; background: rgba(13,13,13,0.97); backdrop-filter: blur(12px); border-top: 2px solid var(--green); padding: 0.85rem 1.5rem calc(0.85rem + env(safe-area-inset-bottom)); display: flex; align-items: center; justify-content: space-between; gap: 1rem; transform: translateY(100%); transition: transform 0.4s cubic-bezier(.22,.68,0,1.2); }
    .sticky-bar.visible { transform: translateY(0); }
    .sticky-title { font-family: 'Bebas Neue', sans-serif; font-weight: 400; font-size: 1.1rem; letter-spacing: 0.06em; color: var(--white); }
    .sticky-sub { font-family: 'DM Mono', monospace; font-size: 0.56rem; color: var(--green); margin-top: 0.12rem; letter-spacing: 0.1em; text-transform: uppercase; }
    .sticky-register-btn { display: inline-flex; align-items: center; justify-content: center; gap: 0.4rem; padding: 0.65rem 1.4rem; border-radius: 2px; text-decoration: none; background: #f0c000; color: var(--ink); font-family: 'DM Sans', sans-serif; font-weight: 700; font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.12em; white-space: nowrap; transition: background 0.2s, transform 0.15s; }
    .sticky-register-btn:hover { background: #d4aa00; transform: translateY(-1px); }

    /* ── FOOTER ── */
    .site-footer { background: var(--ink); border-top: 1px solid rgba(255,255,255,0.08); padding: 4rem 0 2rem; }
    .footer-inner { max-width: 1280px; margin: 0 auto; padding: 0 2.5rem; }
    .footer-top { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; padding-bottom: 3rem; border-bottom: 1px solid rgba(255,255,255,0.08); align-items: start; }
    .footer-col-label { font-family: 'DM Mono', monospace; font-size: 0.55rem; letter-spacing: 0.25em; text-transform: uppercase; color: var(--green); margin-bottom: 1.2rem; display: flex; align-items: center; gap: 0.5rem; }
    .footer-col-label::before { content: ''; display: inline-block; width: 14px; height: 1px; background: var(--green); flex-shrink: 0; }
    .footer-col-label.right { justify-content: flex-end; }
    .footer-col-label.right::before { display: none; }
    .footer-col-label.right::after { content: ''; display: inline-block; width: 14px; height: 1px; background: var(--green); flex-shrink: 0; }
    .footer-links { list-style: none; display: flex; flex-direction: column; gap: 0; }
    .footer-links li a { font-size: 0.9rem; color: rgba(255,255,255,0.42); text-decoration: none; padding: 0.42rem 0; display: block; transition: color 0.2s, padding-left 0.2s; }
    .footer-links li a:hover { color: var(--white); padding-left: 6px; }
    .footer-right-col { text-align: right; }
    .footer-address-lines { display: flex; flex-direction: column; gap: 0.22rem; }
    .footer-address-lines p, .footer-address-lines a { font-family: 'DM Mono', monospace; font-size: 0.68rem; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(255,255,255,0.42); line-height: 1.7; text-decoration: none; display: block; transition: color 0.2s; }
    .footer-address-lines a:hover { color: var(--green); }
    .footer-bottom-row { padding-top: 2.5rem; display: flex; flex-direction: column; align-items: center; gap: 1.1rem; }
    .footer-logo { display: flex; align-items: center; gap: 0.75rem; text-decoration: none; }
    .footer-logo-mark { width: 40px; height: 40px; border-radius: 2px; border: 1px solid rgba(255,255,255,0.1); overflow: hidden; display: flex; align-items: center; justify-content: center; background: rgba(255,255,255,0.06); transition: border-color 0.2s; }
    .footer-logo:hover .footer-logo-mark { border-color: var(--green); }
    .footer-logo-mark img { width: 100%; height: 100%; object-fit: cover; }
    .footer-logo-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.35rem; font-weight: 400; letter-spacing: 0.1em; color: var(--white); line-height: 1; }
    .footer-logo-sub { font-family: 'DM Mono', monospace; font-size: 0.44rem; text-transform: uppercase; letter-spacing: 0.22em; color: var(--green); display: block; margin-top: 2px; }
    .footer-socials { display: flex; align-items: center; gap: 0.55rem; }
    .footer-social-icon { width: 32px; height: 32px; border-radius: 50%; display: flex; align-items: center; justify-content: center; text-decoration: none; flex-shrink: 0; transition: transform 0.2s, opacity 0.2s; }
    .footer-social-icon:hover { transform: translateY(-3px); opacity: 0.85; }
    .footer-social-icon.linkedin { background: #0a66c2; }
    .footer-social-icon.facebook { background: #1877f2; }
    .footer-social-icon.instagram { background: radial-gradient(circle at 30% 107%, #fdf497 0%, #fd5949 45%, #d6249f 60%, #285AEB 90%); }
    .footer-social-icon svg { width: 14px; height: 14px; fill: white; }
    .footer-copyright { font-family: 'DM Mono', monospace; font-size: 0.5rem; text-transform: uppercase; letter-spacing: 0.22em; color: rgba(255,255,255,0.18); text-align: center; }
    @media (max-width: 640px) { .footer-top { grid-template-columns: 1fr; } .footer-right-col { text-align: left; } .footer-col-label.right { justify-content: flex-start; } .footer-col-label.right::before { display: inline-block; } .footer-col-label.right::after { display: none; } .footer-address-lines { text-align: left; } .footer-inner { padding: 0 1.25rem; } }

    /* ── TICKET SECTION ── */
    .ticket-section { padding: 5.5rem 0; background: var(--ink); }
    .ticket-section-inner { max-width: 480px; margin: 0 auto; padding: 0 2rem; }
    .ticket-section .sec-title { color: var(--white); }
    .ticket-section .sec-eyebrow { color: var(--green); }
    .ticket-section .hero-event-card { border-color: rgba(255,255,255,0.18); }
    @media (max-width: 640px) { .ticket-section-inner { padding: 0 1.25rem; } }

    /* ── GLOBAL ANIMATIONS ── */
    @keyframes fadeUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  </style>

  <!-- Meta Pixel -->
  <script>
    !function(f,b,e,v,n,t,s)
    {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};
    if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
    n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t,s)}(window, document,'script',
    'https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', '1507001663644263');
    fbq('track', 'PageView');
    fbq('track', 'ViewContent', {
      content_name: 'AI Agent Workshop - 13 June',
      content_category: 'Workshop',
      value: 365,
      currency: 'SEK'
    });
  </script>
  <noscript>
    <img height="1" width="1" style="display:none"
    src="https://www.facebook.com/tr?id=1507001663644263&ev=PageView&noscript=1"/>
  </noscript>
  <!-- End Meta Pixel -->
</head>
<body>

<!-- ── PUBLICATION STRIP — MARQUEE ── -->
<div class="pub-strip">
  <div class="pub-marquee">
    <span class="pub-marquee-item">Genaition · Workshop Series <span class="dot"></span> Workshop No. 06 · 2026 <span class="dot"></span> Online · 13 June 2026 <span class="dot"></span> Limited to 25 seats <span class="dot"></span> Early Bird ends 9 May <span class="dot"></span></span>
    <span class="pub-marquee-item">Genaition · Workshop Series <span class="dot"></span> Workshop No. 06 · 2026 <span class="dot"></span> Online · 13 June 2026 <span class="dot"></span> Limited to 25 seats <span class="dot"></span> Early Bird ends 9 May <span class="dot"></span></span>
  </div>
</div>

<!-- ── SITE NAV ── -->
<nav class="site-nav" id="siteNav">
  <div class="nav-inner">
    <a href="/index.html" class="logo">
      <div class="logo-mark">
        <img src="/android-chrome-512x512.png" alt="Genaition" onerror="this.style.display='none';" />
      </div>
      <div>
        <span class="logo-name">Genaition</span>
        <span class="logo-sub">Workshops — AI Agents</span>
      </div>
    </a>
    <nav class="nav-links">
      <a href="#about">Overview</a>
      <a href="#build">What you'll build</a>
      <a href="#agenda">Agenda</a>
      <a href="#instructor">Instructor</a>
      <a href="#faq">FAQ</a>
    </nav>
    <div class="nav-actions">
      <div class="lang-wrap" id="langWrap">
        <button class="lang-btn" id="langBtn" aria-haspopup="true" aria-expanded="false">
          <img src="/english.png" alt="English" />
          <span>EN</span>
          <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="lang-menu" id="langMenu">
          <a href="/index.html"><img src="/english.png" alt="English flag" /> English</a>
          <a href="/event-1-sv.html"><img src="/swedish.png" alt="Swedish flag" /> Svenska</a>
        </div>
      </div>
      <a href="#ticket-box" class="nav-signup">Sign-Up</a>
    </div>
  </div>
</nav>

<!-- ── HERO ── -->
<section id="top" class="hero">
  <div class="hero-grid">

    <div class="hero-left">
      <span class="hero-eyebrow"><span class="eyebrow-dot"></span> Early Bird · Ends 9 May</span>

      <h1 class="hero-title" style="animation: fadeUp 0.7s ease 0.2s both;">
        <span class="t-ink">Build your</span>
        <span class="t-ink">First</span>
        <span class="t-yellow">AI Agent</span>
      </h1>

      <blockquote class="hero-quote">
        Leave in 3 hours with a live AI email agent you built yourself, no coding required. Join online from anywhere on 13 June.
      </blockquote>

      <a href="#ticket-box" class="ticket-cta hero-mobile-cta">
        Sign-up
      </a>

      <!-- LIVE COUNTDOWN -->
      <div class="hero-countdown" id="heroCountdown">
        <span class="countdown-label">Starts in</span>
        <div class="countdown-unit"><span class="countdown-num" id="cdDays">48</span><span class="countdown-sub">Days</span></div>
        <span class="countdown-sep">:</span>
        <div class="countdown-unit"><span class="countdown-num" id="cdHours">09</span><span class="countdown-sub">Hours</span></div>
        <span class="countdown-sep">:</span>
        <div class="countdown-unit"><span class="countdown-num" id="cdMins">22</span><span class="countdown-sub">Mins</span></div>
        <span class="countdown-sep">:</span>
        <div class="countdown-unit"><span class="countdown-num" id="cdSecs">41</span><span class="countdown-sub">Secs</span></div>
      </div>

      <div class="hero-rule"></div>

      <div class="hero-meta">
        <div class="meta-block">
          <p class="meta-label">Date</p>
          <p class="meta-value">13 June</p>
        </div>
        <div class="meta-block">
          <p class="meta-label">Time</p>
          <p class="meta-value">13:30–16:30</p>
        </div>
        <div class="meta-block">
          <p class="meta-label">Format</p>
          <p class="meta-value yellow">Live Online</p>
        </div>
        <div class="meta-block">
          <p class="meta-label">Seats</p>
          <p class="meta-value">Max 25</p>
        </div>
      </div>
    </div>


  </div>
</section>


<!-- ── ABOUT ── -->
<section id="about" class="section section-alt">
  <div class="container">
    <p class="sec-eyebrow reveal">Who this is for</p>
    <h2 class="sec-title reveal reveal-delay-1">Founders<br>and Operators</h2>
    <p class="sec-desc reveal reveal-delay-2">Hands-on workshop where you build and launch your own AI Email Agent in 3 hours. No coding required — just bring a laptop. Leave with a working agent and a clear mental model you can reuse for real work.</p>
    <div class="sec-rule"></div>
  </div>
  <div class="dark-strip">
    <div class="benefit-dark-grid">
      <div class="benefit-dark-card reveal">
        <div class="bdc-icon">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
        </div>
        <p class="bdc-title">Ship in 3 hours</p>
        <p class="bdc-desc">Build a real email agent in 3 hours. Walk out with a working assistant, not slides, not theory.</p>
      </div>
      <div class="benefit-dark-card reveal reveal-delay-1">
        <div class="bdc-icon">
          <svg viewBox="0 0 24 24"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <p class="bdc-title">You set the rules</p>
        <p class="bdc-desc">Instruct your agent. The AI follows them. Know exactly what it sends, why, and when.</p>
      </div>
      <div class="benefit-dark-card reveal reveal-delay-2">
        <div class="bdc-icon">
          <svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        </div>
        <p class="bdc-title">Real agent, built by you</p>
        <p class="bdc-desc">See how ReAct, LLM API, and system prompts actually work by building one yourself.</p>
      </div>
      <div class="benefit-dark-card reveal reveal-delay-3">
        <div class="bdc-icon">
          <svg viewBox="0 0 24 24"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
        </div>
        <p class="bdc-title">Hands-on help</p>
        <p class="bdc-desc">Max 25 seats — small cohort, structured pace, and support until your agent actually works.</p>
      </div>
    </div>
    <div class="dark-strip-footer">
      <div class="tech-reveal">
        <button class="tech-reveal-btn" id="techBtn">
          <span>Your tech stack</span>
          <svg viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg>
        </button>
        <div class="tech-reveal-body" id="techBody">
          <div class="tech-reveal-inner">
            <strong>n8n Cloud</strong> — the automation backbone that connects your tools and runs the entire pipeline.<br>
            <strong>Claude Sonnet 4.5</strong> — reads emails, writes replies in your tone, and checks quality before anything is sent.<br>
            <strong>n8n MCP</strong> — gives the agent context and memory so it knows who it's talking to and what to do next.<br>
            <strong>ReAct Workflow</strong> — the reasoning loop that lets the agent think, decide, and act step by step.<br><br>
            Pre-configured so you spend time customising, not troubleshooting.
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── WHAT YOU'LL BUILD ── -->
<section id="build" class="section">
  <div class="container">
    <p class="sec-eyebrow reveal">What you'll build</p>
    <h2 class="sec-title reveal reveal-delay-1">AI outreach pipeline —<br><em>ready to use right away</em></h2>
    <p class="sec-desc reveal reveal-delay-2">Detect the lead → profiling and analyzing → generating email text → send and update the document.</p>
    <div class="build-rows">
      <div class="build-row">
        <div class="build-photo-side">
          <img src="/email-workshopm.jpg" alt="Workshop participants at laptops" />
          <p class="build-photo-label">Stockholm · Workshop cohort</p>
        </div>
        <div class="build-text-side">
          <div class="build-num">01</div>
          <p class="build-title">Hands-on AI agent</p>
          <p class="build-desc">No slides. No lecture. You'll build "Ove", a live email agent that pulls contacts, writes personalized emails, sends via Gmail, and logs results — autonomously.</p>
          <ul class="build-checks">
            <li>Prompts that adapt to each contact automatically</li>
            <li>Let AI copy your tone — formal, casual, or brand voice?</li>
            <li>One-click reuse, take it straight into your next project</li>
          </ul>
        </div>
      </div>
      <div class="build-divider"></div>
      <div class="build-row reverse">
        <div class="build-photo-side">
          <img src="/workshop999.jpg" alt="Workshop group photo" style="object-position: center top;" />
          <p class="build-photo-label">Göteborg · Sep 2025</p>
        </div>
        <div class="build-text-side">
          <div class="build-num">02</div>
          <p class="build-title">Real-world scenarios</p>
          <p class="build-desc">See how founders and teams use agents to warm prospects, follow up, and save hours weekly. Leave with a plan you can apply immediately.</p>
          <ul class="build-checks">
            <li>Outreach that gets replies</li>
            <li>Quality checks so AI never sends something wrong</li>
            <li>Plug and play templates for sales, recruiting, and ops</li>
          </ul>
        </div>
      </div>
      <div class="build-divider"></div>
      <div class="build-row">
        <div class="build-photo-side">
          <img src="/email-workshop.jpg" alt="Workshop in-room session" />
          <p class="build-photo-label">Online · June 2026</p>
        </div>
        <div class="build-text-side">
          <div class="build-num">03</div>
          <p class="build-title">Small online cohort</p>
          <p class="build-desc">Join a small online cohort of entrepreneurs, freelancers, and builders who are done waiting for AI to make sense. All you need is a laptop and a browser.</p>
          <ul class="build-checks">
            <li>Bring your laptop — build together</li>
            <li>Certificate of completion</li>
            <li>Q&amp;A throughout the session</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── AGENDA ── -->
<section id="agenda" class="section section-alt">
  <div class="container">
    <p class="sec-eyebrow reveal">Agenda</p>
    <h2 class="sec-title reveal reveal-delay-1">13 June · 13:30–16:30</h2>
    <p class="sec-desc reveal reveal-delay-2">We move fast but stay beginner-friendly. You follow proven templates, understand the logic, and customise everything for your own inbox.</p>
    <div class="agenda-list">
      <div class="agenda-item reveal"><div class="agenda-time-col"><span class="agenda-time">13:30</span></div><div class="agenda-content"><p class="agenda-topic">Intro &amp; Setup</p><p class="agenda-detail">Connect your tools, meet your cohort. Everything pre-configured, no installs, no friction.</p></div></div>
      <div class="agenda-item reveal reveal-delay-1"><div class="agenda-time-col"><span class="agenda-time">13:50</span></div><div class="agenda-content"><p class="agenda-topic">Plug-in and test</p><p class="agenda-detail">Connect Gmail and n8n. Run your first live trigger and watch the agent read a real email.</p></div></div>
      <div class="agenda-item reveal reveal-delay-2"><div class="agenda-time-col"><span class="agenda-time">14:30</span></div><div class="agenda-content"><p class="agenda-topic">Build your agent</p><p class="agenda-detail">Start from the template and extend it — set rules, write your first system prompt, run a live send.</p></div></div>
      <div class="agenda-item reveal"><div class="agenda-time-col"><span class="agenda-time">15:00</span></div><div class="agenda-content"><p class="agenda-topic">10-min break</p><p class="agenda-detail">Step away, grab a coffee, then come back ready to build from scratch.</p></div></div>
      <div class="agenda-item reveal reveal-delay-1"><div class="agenda-time-col"><span class="agenda-time">15:10</span></div><div class="agenda-content"><p class="agenda-topic">Building from scratch</p><p class="agenda-detail">Rebuild the agent from the ground up — you set the rules, the tone, and the logic.</p></div></div>
      <div class="agenda-item reveal reveal-delay-2"><div class="agenda-time-col"><span class="agenda-time">16:15</span></div><div class="agenda-content"><p class="agenda-topic">Q&amp;A + Certificate</p><p class="agenda-detail">Live Q&amp;A, personalisation tips, and your certificate of completion.</p></div></div>
    </div>
  </div>
</section>

<!-- ── TICKET ── -->
<section class="ticket-section section-alt" id="ticket-box">
  <div class="ticket-section-inner">
    <div class="hero-event-card reveal">
      <div class="hero-ticket">
        <p class="ticket-price-old">595 SEK</p>
        <div class="ticket-price-row">
          <span class="ticket-price">365 SEK</span>
          <span class="ticket-save">Save 230 SEK</span>
        </div>
        <div class="ticket-divider"></div>
        <ul class="ticket-features">
          <li>Full 3-hour live online workshop</li>
          <li>Hands-on help until your agent works</li>
          <li>Post-workshop 1:1 support</li>
          <li>Templates, prompts &amp; workflow files</li>
          <li>Certificate of completion</li>
        </ul>

        <a href="/before-checkout.html" class="ticket-cta">
          Sign Up
        </a>
        <p class="ticket-note">Secure checkout — Max 25 seats — 13 June 2026</p>
        <p class="ticket-note" style="margin-top:0.3rem;">Need an invoice? Email <a href="mailto:info@genaition.org">info@genaition.org</a></p>
      </div>
    </div>
  </div>
</section>

<!-- ── INSTRUCTOR ── -->
<section id="instructor" class="section">
  <div class="container">
    <p class="sec-eyebrow reveal">Your instructor</p>
    <h2 class="sec-title reveal reveal-delay-1">Javid Valiyev</h2>
    <div class="instructor-grid">
      <div class="instructor-photo-wrap reveal">
        <img src="/workshop999.jpg" alt="Javid Valiyev" onerror="this.style.background='#e8e2d2';this.style.minHeight='280px';this.style.display='block';" />
        <div class="instructor-plate">
          <p class="instructor-name">Javid Valiyev</p>
          <p class="instructor-role">Founder — Genaition</p>
          <div class="instructor-badges">
            <span class="ibadge">5+ years AI</span>
            <span class="ibadge">150+ trained</span>
          </div>
        </div>
      </div>
      <div class="reveal reveal-delay-2">
        <p class="instructor-bio">Javid builds Applied AI and teaches people and businesses how to implement them without needing a developer. His workshops are known for being hands-on, structured, and beginner-safe — you don't leave without a working system.</p>
        <p class="instructor-bio">He is the founder of Genaition and has led 5 workshops across Sweden with over 150 professionals trained.</p>
        <blockquote class="instructor-pullquote">"You don't leave without a working system. That's the rule."</blockquote>
      </div>
    </div>
  </div>
</section>

<!-- ── GALLERY ── -->
<section class="gallery-section">
  <div style="max-width:760px;margin:0 auto;padding:0 2rem;">
    <div class="gallery-header reveal">
      <div>
        <p class="sec-eyebrow" style="margin-bottom:0.4rem;">From our workshops</p>
        <h2 class="sec-title" style="font-size:clamp(1.6rem,3.5vw,2.4rem);">How we teach</h2>
      </div>
      <p class="gallery-count">Genaition · Stockholm · Workshops 01–05</p>
    </div>
  </div>
  <div class="gallery-grid">
    <div class="gallery-slot-a reveal">
      <img src="/workshop13.jpg" alt="Genaition workshop — building session" style="object-position: center top;" onerror="this.parentElement.style.background='#e8e2d2';" />
      <div class="gallery-img-label">Online · June 2026</div>
    </div>
    <div class="gallery-slot-b reveal reveal-delay-1">
      <img src="/workshop15.jpg" alt="Genaition workshop — group session" onerror="this.parentElement.style.background='#e0dace';" />
      <div class="gallery-img-label">Stockholm · March 2026</div>
    </div>
    <div class="gallery-slot-c reveal reveal-delay-2">
      <img src="/workshop16.jpg" alt="Genaition workshop — participants" style="object-position: center center;" onerror="this.parentElement.style.background='#ddd8c8';" />
      <div class="gallery-img-label">Göteborg · SEP 2025</div>
    </div>
  </div>
</section>

<!-- ── TESTIMONIALS ── -->
<section class="tp-section">
  <div class="container">
    <p class="sec-eyebrow reveal">What people say</p>
    <div class="reveal reveal-delay-1" style="display:flex;align-items:center;gap:0.6rem;margin-bottom:2rem;">
      <div style="display:flex;gap:3px;">
        <div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div>
        <div class="tp-star" style="background:linear-gradient(90deg,#1a6b3a 50%,rgba(26,18,8,0.08) 50%);"></div>
      </div>
      <span style="font-family:'DM Mono',monospace;font-size:0.6rem;color:var(--ink-3);letter-spacing:0.08em;">4.5 / 5 · Trustpilot</span>
      <a href="https://www.trustpilot.com/review/genaition.io" target="_blank" style="font-family:'DM Mono',monospace;font-size:0.58rem;letter-spacing:0.14em;text-transform:uppercase;color:var(--green);text-decoration:none;border-bottom:1px solid var(--green-border);padding-bottom:1px;margin-left:auto;">See all reviews ↗</a>
    </div>
    <div class="tp-cards reveal reveal-delay-2">
      <div class="tp-card">
        <div class="tp-card-stars"><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div></div>
        <span class="tp-quotemark">"</span>
        <p class="tp-card-text">The workshop was very good and helpful. The trainer guided us clearly in each step and everything was easy to follow.</p>
        <div><p class="tp-card-name">Valentina Fermanelli</p><p class="tp-card-date">Workshop · Göteborg · 2025</p></div>
      </div>
      <div class="tp-card">
        <div class="tp-card-stars"><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div></div>
        <span class="tp-quotemark">"</span>
        <p class="tp-card-text">Great session! Well-demonstrated with practical understanding and real business scenarios throughout.</p>
        <div><p class="tp-card-name">Sara Svensson</p><p class="tp-card-date">Workshop · Stockholm · 2026</p></div>
      </div>
      <div class="tp-card">
        <div class="tp-card-stars"><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div><div class="tp-star"></div></div>
        <span class="tp-quotemark">"</span>
        <p class="tp-card-text">I left feeling very inspired. A big insight into what Agentic AI actually is and how it works in practice.</p>
        <div><p class="tp-card-name">Joey Sorel</p><p class="tp-card-date">Workshop · Stockholm · 2025</p></div>
      </div>
    </div>
  </div>
</section>

<!-- ── FAQ ── -->
<section id="faq" class="section section-alt">
  <div class="container">
    <p class="sec-eyebrow reveal">FAQ</p>
    <h2 class="sec-title reveal reveal-delay-1">Common questions</h2>
    <p class="sec-desc reveal reveal-delay-2">Still wondering? Email <a href="mailto:info@genaition.org" style="color:var(--green);text-decoration:none;border-bottom:1px solid var(--green-border);">info@genaition.org</a> — we reply fast.</p>
    <div class="faq-list reveal">
      <div class="faq-item"><button class="faq-btn"><span>I have no experience with AI or coding. Is this for me?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Yes — this is built for beginners. You follow step-by-step, we help anyone who gets stuck, and you leave with a working agent. No coding required.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>What exactly will I build?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">An AI Reply Agent: it reads incoming emails, understands what the sender wants, drafts a reply in your tone, runs a quality check, and then either flags it for your approval or sends directly — for categories you define as safe. You control the rules.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>Will this send emails automatically without me seeing them?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Only if you explicitly set it to. The default setup is human-in-the-loop — you review and approve before anything is sent. You define which categories are safe to auto-send.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>What do I need to bring?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Your laptop and a Google account (Gmail). We'll set up all the other tools together during the first 20 minutes. No accounts to create in advance.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>Can I apply this to my own work after the workshop?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Yes. You take the templates and workflow files with you. We cover support, sales, recruiting, and ops inboxes — pick the one closest to your work and customise it live.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>What is the refund and cancellation policy?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Full refund if you cancel at least 7 days before the event. After that, you can transfer your ticket to a colleague. Email <a href="mailto:info@genaition.org">info@genaition.org</a> to arrange either.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>Can my company pay? Do you provide invoices?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Yes. If you need an invoice, want to book multiple seats, or need a team discount, email <a href="mailto:info@genaition.org">info@genaition.org</a> and we'll sort it out — usually within 1 business day.</div></div></div>
      <div class="faq-item"><button class="faq-btn"><span>After the workshop, what will n8n Cloud and Claude API cost me monthly?</span><svg class="faq-icon" viewBox="0 0 20 20" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="10" y1="3" x2="10" y2="17"/><line x1="3" y1="10" x2="17" y2="10"/></svg></button><div class="faq-body"><div class="faq-body-inner">Very little. n8n Cloud starts at around €20/month (~220 SEK), and Claude API usage for a typical email inbox runs €5–15/month. Total: roughly 150–350 SEK/month. You can also self-host n8n for free.</div></div></div>
    </div>
  </div>
</section>

<!-- ── FOOTER ── -->
<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-top">
      <div>
        <p class="footer-col-label">Links</p>
        <ul class="footer-links">
          <li><a href="/index.html">Home</a></li>
          <li><a href="/about.html">About us</a></li>
          <li><a href="/events.html">Events</a></li>
          <li><a href="/contact.html">Contact</a></li>
        </ul>
      </div>
      <div class="footer-right-col">
        <p class="footer-col-label right">GenAition</p>
        <div class="footer-address-lines">
          <p>Konstgjutarvägen 4</p>
          <p>121 44 Stockholm</p>
          <p>Sweden</p>
          <a href="mailto:info@genaition.org" style="margin-top:0.5rem;">info@genaition.org</a>
          <a href="tel:+46762608848">+46 762 608 848</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom-row">
      <a href="/index.html" class="footer-logo">
        <div class="footer-logo-mark"><img src="/android-chrome-512x512.png" alt="Genaition" onerror="this.style.display='none'" /></div>
        <div><span class="footer-logo-name">Genaition</span><span class="footer-logo-sub">Workshops · AI Agents</span></div>
      </a>
      <div class="footer-socials">
        <a href="https://linkedin.com/company/genaition" target="_blank" class="footer-social-icon linkedin" aria-label="LinkedIn"><svg viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg></a>
        <a href="https://facebook.com/genaition" target="_blank" class="footer-social-icon facebook" aria-label="Facebook"><svg viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg></a>
        <a href="https://instagram.com/genaition" target="_blank" class="footer-social-icon instagram" aria-label="Instagram"><svg viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881z"/></svg></a>
      </div>
      <p class="footer-copyright">© 2026 GenAition. All rights reserved.</p>
    </div>
  </div>
</footer>

<!-- ── STICKY BAR ── -->
<div class="sticky-bar" id="stickyBar">
  <div>
    <p class="sticky-title">AI Agent Workshop</p>
    <p class="sticky-sub">Online · 13 June · Early Bird Active</p>
  </div>
  <a href="#ticket-box" class="sticky-register-btn">Sign up - 365 SEK</a>
</div>

<script>
  // ── SCROLL REVEAL ──
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); observer.unobserve(e.target); } });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  revealEls.forEach(el => observer.observe(el));

  // ── NAV SCROLL SHADOW ──
  const nav = document.getElementById('siteNav');
  window.addEventListener('scroll', () => { nav.classList.toggle('scrolled', window.scrollY > 40); }, { passive: true });

  // ── LANGUAGE DROPDOWN ──
  const langWrap = document.getElementById('langWrap');
  const langBtn  = document.getElementById('langBtn');
  function closeLang() { langWrap && langWrap.classList.remove('open'); langBtn && langBtn.setAttribute('aria-expanded','false'); }
  function toggleLang() { const open = !langWrap.classList.contains('open'); langWrap.classList.toggle('open', open); langBtn.setAttribute('aria-expanded', open ? 'true' : 'false'); }
  if (langBtn) {
    langBtn.addEventListener('click', e => { e.stopPropagation(); toggleLang(); });
    document.addEventListener('click', closeLang);
    document.addEventListener('keydown', e => { if (e.key === 'Escape') closeLang(); });
  }

  // ── FAQ ──
  document.querySelectorAll('.faq-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const item = btn.parentElement;
      const body = item.querySelector('.faq-body');
      const isOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item').forEach(i => { i.classList.remove('open'); i.querySelector('.faq-body').style.maxHeight = '0'; });
      if (!isOpen) { item.classList.add('open'); body.style.maxHeight = body.scrollHeight + 'px'; }
    });
  });

  // ── TECH REVEAL ──
  const techBtn = document.getElementById('techBtn');
  const techBody = document.getElementById('techBody');
  if (techBtn && techBody) {
    techBtn.addEventListener('click', () => {
      const isOpen = techBtn.classList.contains('open');
      techBtn.classList.toggle('open', !isOpen);
      techBody.style.maxHeight = isOpen ? '0' : techBody.scrollHeight + 'px';
    });
  }

  // ── COUNTDOWN TIMER ──
  function updateCountdown() {
    const target = new Date('2026-06-13T13:30:00+02:00');
    const now = new Date();
    const diff = target - now;
    if (diff <= 0) { document.getElementById('heroCountdown').style.display = 'none'; return; }
    const days  = Math.floor(diff / 86400000);
    const hours = Math.floor((diff % 86400000) / 3600000);
    const mins  = Math.floor((diff % 3600000) / 60000);
    const secs  = Math.floor((diff % 60000) / 1000);
    document.getElementById('cdDays').textContent  = String(days).padStart(2,'0');
    document.getElementById('cdHours').textContent = String(hours).padStart(2,'0');
    document.getElementById('cdMins').textContent  = String(mins).padStart(2,'0');
    document.getElementById('cdSecs').textContent  = String(secs).padStart(2,'0');
  }
  updateCountdown();
  setInterval(updateCountdown, 1000);

  // ── STICKY BAR ──
  const stickyBar = document.getElementById('stickyBar');
  const ticketBox = document.getElementById('ticket-box');
  window.addEventListener('scroll', () => {
    const rect = ticketBox.getBoundingClientRect();
    const ticketVisible = rect.top < window.innerHeight && rect.bottom > 0;
    stickyBar.classList.toggle('visible', window.scrollY > 500 && !ticketVisible);
  }, { passive: true });
</script>


</body>
</html>